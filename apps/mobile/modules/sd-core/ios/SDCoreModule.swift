import ExpoModulesCore

public class SDCoreModule: Module {
  public func definition() -> ModuleDefinition {
    Name("SDCore")

    // TODO: We are not using this. We will once the Objective C bridge is pulled out of the IOS project and rewritten to Swift.
  }
}
