import { View } from 'react-native';
import { BrowseStackScreenProps } from '~/navigation/tabs/BrowseStack';

export default function LocationScreen({ navigation, route }: BrowseStackScreenProps<'Browse'>) {
	return <View></View>;
}
