{
    "extends": [
        "//"
    ],
    "pipeline": {
        "gen": {
           "inputs": ["icons/**", "images/**", "scripts/generate.mjs"],
           "outputs": ["icons/**", "images/**", "lottie/**", "deps/**", "svgs/**"]
        }
    }
}